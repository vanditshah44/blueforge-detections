# BLUE-FORGE detection-as-code toolchain (pySigma ecosystem)
sigma-cli>=1.0.0
pysigma>=0.11.0

# Backends: vendor-neutral Sigma -> platform query languages
pysigma-backend-splunk>=1.1.0        # Splunk SPL (secondary SIEM)
pysigma-backend-kusto>=0.4.0         # KQL for Microsoft Sentinel / Defender XDR

# Processing pipelines (field mapping for the target log sources)
pysigma-pipeline-sysmon>=1.0.0
pysigma-pipeline-windows>=1.0.0

# Validation / testing
pyyaml>=6.0
pytest>=7.0
